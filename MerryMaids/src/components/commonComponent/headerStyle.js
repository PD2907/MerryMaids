import {
  StyleSheet,
  Dimensions
} from 'react-native';

export default StyleSheet.create({
  containerStyle: {
    height: 40,
    flexDirection:'row'
  },
  touchableAreaStyle:{
    top: 80, bottom: 80, left: 80, right: 80
  }
 
});